# Minimal deps for serverless/cloud (student-friendly)
# No PyTorch, no local LLM — uses HTTP LLM APIs (Groq, OpenAI, OpenRouter)

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
pydantic>=2.5.0
